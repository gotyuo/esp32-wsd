# Release v7.52 — 修复「扫描局域网扫不到在线设备 / 扫到却无设备信息」

> **发布日期**: 2026-09-20
> **项目**: 重症监护环境监测系统 (ICU EnvMon)
> **架构**: ESP32-S3 / ESP8266 边缘节点 + Docker 后端 + Web 监护台
> **基于**: commit `b3d84b4`（v7.51）

---

## ✨ 变更汇总

排查「设备有实时数据（MQTT 遥测正常），但点『🔍 扫描局域网』返回空 / 偶尔扫到一台却没有任何设备信息」现象，定位 3 个根因并修复：

### 1. 扫描网段在容器里被 Docker 网桥劫持（服务器）
- **根因**：backend 容器是 bridge 网络，`/api/devices/scan` 自动探测 LAN IP 时
  `socket.connect((239.255.1.1,12091))` 返回 docker0 / compose 桥接网段（172.17-172.24.x），
  于是去扫 `172.17.0.0/24` 这类空网段，永远发现不了设备。
  v7.51 修复的是 discovery 应答器（host 网络），main.py 的扫描端点仍受影响。
- **修复**：`_get_lan_ip()` 优先用「已知设备的 ip_addr」（遥测携带的真实 LAN IP）
  反推实际网段；多播/网卡兜底时跳过 Docker 网桥网段；仍可用 `DISC_IP` 显式指定。

### 2. HTTP 门户探测对「已配网设备」不可达即静默扫空（服务器）
- **根因**：扫描只认 HTTP 门户指纹（80/8000/8080/8081/6667 上的 `/api/data`、`/json`）。
  设备虽在 STA 模式开着数据服务，但 AP 隔离 / 容器与无线网段路由不通时，
  服务器反向连不上设备 80 端口 → `found` 为空。
- **修复**：扫描合并「MQTT / 遥测已知设备」作为权威来源——凡是最近
  `OFFLINE_TIMEOUT_S` 秒内有真实遥测、或 broker 保留 `status=online` 的设备，
  直接列为已发现（含 id / name / ip / fw）。设备上报 MQTT 本身就是「在局域网里活着」
  的最强证据，与端口可达性解耦。

### 3. 空壳设备 esp-&lt;ip&gt;（「扫到但没设备信息」）（服务器 + 前端）
- **根因**：HTTP 命中但解析不出 device_id、或仅 TCP80 可达（ping-only，路由器 /
  其他主机），旧代码硬造 `esp-<ip>` 空设备（仅 ping-only 不入库，但仍出现在 found
  列表并被前端当设备提示）。
- **修复**：无法确认为 ESP 设备的响应统一归入 `unidentified_hosts`（不注册、
  不展示为设备）；仅当该 IP 已映射到已知设备时才沿真实 id 展示。
  前端 toast 区分「发现 N 台设备」与「另有 N 个不可识别主机」，不再报出空壳设备。

## 🔧 改动文件

| 文件 | 说明 |
|------|------|
| `server/app/main.py` | scan 端点：MQTT/遥测已知设备合并；LAN IP 探测修正；unidentified_hosts 分离 |
| `server/app/static/index.html` | 扫描结果展示：显示设备名 + 未识别主机单独提示 |
| `.rollback/20260920-scan-mqtt-merge/` | 修复前快照（保留版本，可回滚） |

## 📋 升级指引

重启 backend 容器后，在设备管理页点「🔍 扫描局域网」：应能直接列出当前上报 MQTT
的全部在线设备（含名称 / IP / 固件版本）。若仍为空且确认设备都有实时数据，
检查 `.env` 的 `DISC_IP` 是否被显式指向错误网段（应留空，或用宿主机实际内网 IP 显式指定）。