# Release v7.48 — ICU 环境监测系统 设备管理布局修复版

> **发布日期**: 2026-09-19
> **项目**: 重症监护环境监测系统 (ICU EnvMon)
> **架构**: ESP32-S3 / ESP8266 边缘节点 + Docker 后端 + Web 监护台
> **基于**: commit `61ab3db`

---

## ✨ 变更汇总

本版 1 个提交，修复设备管理页设备卡片操作按钮换行问题。

### 1. 设备管理卡片加宽，操作按钮单行显示
- 设备管理「全部 / 局域网 / 外网」页签下，设备卡片底部共 6 个操作按钮（刷新 / ✏ 编辑 / ⚙ 配置 / 🔗 绑定 / 🔓 解绑 / 删除），原先在 300px 最小列宽下放不下，✏ 编辑被挤到第二行
- 通过 `#devOnline` / `#devOffline` 限定作用域：设备卡片最小宽度提升至 **440px**（`min(440px,100%)`，小屏单列时收缩为整宽，不横向溢出），并对该页按钮行启用 `flex-wrap:nowrap` 双保险
- 未改动任何 onclick / JS 逻辑；患者管理 `#pGrid`、设备接入快照 `access*Grid` 等共用 `.devgrid` 的界面布局**零影响**

## 🔧 改动文件

| 文件 | 说明 |
|------|------|
| `server/app/static/index.html` | 设备管理页卡片宽度 +2 行 CSS |
| `.rollback/20260919-devcard-actions-width/` | 修复前 index.html 快照（保留版本，可回滚） |