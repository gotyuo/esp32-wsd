# Release v7.47 — ICU 环境监测系统 监护体验优化版

> **发布日期**: 2026-09-17
> **项目**: 重症监护环境监测系统 (ICU EnvMon)
> **架构**: ESP32-S3 / ESP8266 边缘节点 + Docker 后端 + Web 监护台
> **基于**: commit `05b3a0c`（v7.46.27 之后）

---

## ✨ 变更汇总

本版共 **9 个提交**，聚焦大屏投屏与实时监护体验，全部经真实数据库 + 无头浏览器验证。

### 1. 大屏免登录投屏
- `/api/dashboard` 改为**公开端点**（代码注释原本就写"公开，无需登录，适合投屏"，v7.46 补鉴权后投屏场景失效）
- `dashboard.html` 请求自动携带登录 token（已登录时）；未登录打开 `http://<host>:12090/dashboard` 也能直接看到大屏
- ⚠️ 注意：公开后任何拿到 URL 的人可见患者姓名/体征/报警，建议仅限投屏局域网使用

### 2. 体征趋势图重构
- **一指标一小图**（mini-multiples）：心率/血氧/呼吸/收缩压/舒张压/体温/血糖/湿度/气压各自独立小图与量程，不再 9 条曲线挤一张 200px 图
- 小图**单列整宽**，与用药时间线图表同宽
- 曲线**按数据缺口分段绘制 + 数据点圆点标记**：稀疏数据不再用长直线"补"过整段空缺
- 新增时间档位：**30分 / 1时 / 3时 / 6时 / 12时 / 24时**；后端 `hours` 参数支持小数；前端按窗口过滤，短窗口空数据诚实显示"暂无"，不混入窗口外旧数据

### 3. 出入量与底部面板按钮
- `+ 出入量` 拆分为 **`+ 入量`（蓝）/ `+ 出量`（红）** 两个按钮，弹窗自动预选方向
- 出入量 / 用药 / 检查 / 检验 四个面板按钮风格统一：**主操作彩色前置，刷新/历史灰色分组靠右**，中间分隔线

### 4. 最近录入增强
- 标题字号放大加粗，容器高度放开，**10 条记录完整显示**不再截断滚动
- 新增 **「更多最近录入…」** 按钮 → 体征录入历史弹窗：拉取近 1 年数据，**每页 10 条**，`共 N 条，第 x / y 页`，带上一页/下一页（样式对齐报警记录分页），管理员可直接删除

---

## 🔧 改动文件

| 文件 | 说明 |
|------|------|
| `server/app/static/dashboard.html` | 大屏请求带 token + 未登录提示区分 |
| `server/app/static/index.html` | 实时监护全部 UI 改动（趋势图/按钮/最近录入） |
| `server/app/main.py` | `/api/dashboard` 免登录；`hours` 参数支持小数(≥0.1) |

## 🚀 部署要点

- `index.html` / `dashboard.html`：`docker cp` 热更进容器即可，无需重启，浏览器**强制刷新**（Ctrl+Shift+R）
- `main.py`：替换后**必须重启容器**；启动时自动做 schema 补齐
- 或直接在部署目录 `git pull` 后 `docker compose up -d --build`


### 5. 体征趋势空窗回退 + 用药时间线同款档位（同日补充）
- 体征趋势：所选窗口（如 30分/1时）内无新数据时，**回退显示最近一次记录**（各指标最近值 + 最新时间），并在图上方给出橙色提示，不再显示空白"暂无"
- 用药时间线：新增与体征趋势一致的 **30分 / 1时 / 3时 / 6时 / 12时 / 24时** 六档时间切换


## 📌 提交列表

| commit | 说明 |
|--------|------|
| `c2375cf` | 大屏免登录投屏 + dashboard 前端带 token 鉴权 |
| `3da2a41` | 体征趋势改为一指标一小图，避免多量纲曲线/标签拥挤 |
| `481921c` | 体征趋势小图改为单列整宽，与用药时间线图表宽度一致 |
| `9d514b8` | 出入量按钮拆分——入量/出量两个独立按钮，弹窗自动预选方向 |
| `ca1878d` | 出入量面板按钮重排+入蓝出红配色，刷新/历史分组靠右 |
| `1a2b9a2` | 用药/检查/检验面板按钮统一风格——主操作蓝色+维护按钮靠右分组 |
| `f42056f` | 最近录入标题放大+高度显示全10条，新增更多按钮与每页10条分页历史弹窗 |
| `9da8336` | 体征趋势曲线按数据缺口分段绘制+数据点标记，避免稀疏数据长直线连补失踪段 |
| `05b3a0c` | 体征趋势新增30分/1时/3时/6时/12时/24时档位，后端hours支持小数，前端窗口过滤防旧数据混入 |
| `（下一提交）` | 体征趋势空窗回退显示最近记录+提示条；用药时间线新增同款六档时间切换 |